name = "Hhsqoo/artifactdock"

version = "0.1.0"

source = "src"

readme = "README.md"

repository = "https://github.com/Hhsqoo/artifactdock"

license = "Apache-2.0"

description = "A lightweight MoonBit-native OCI Distribution registry server"

keywords = [ "oci", "registry", "wasm", "container", "artifacts" ]

preferred_target = "native"

supported_targets = "+native"

import {
  "moonbitlang/async@0.21.3",
  "gmlewis/sha256@0.17.33",
}
